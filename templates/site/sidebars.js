// @ts-check
/**
 * The guides sidebar is generated from the folder structure, so a new guide
 * appears automatically as soon as `guidesmith generate` writes its MDX file.
 * Ordering comes from each guide's `sidebar_position` frontmatter.
 *
 * @type {import('@docusaurus/plugin-content-docs').SidebarsConfig}
 */
const sidebars = {
  guidesSidebar: [
    'intro',
    {
      type: 'category',
      label: 'User guides',
      collapsed: false,
      link: { type: 'generated-index', title: 'User guides', description: 'Step-by-step walkthroughs.' },
      items: [{ type: 'autogenerated', dirName: 'guides' }],
    },
  ],
};

export default sidebars;
